export const title = 'align-self'
export const description = 'Utilities for controlling how an individual flex or grid item is positioned along its container\'s cross axis.'

## align-self

Utilities for controlling how an individual flex or grid item is positioned along its container's cross axis.

| Utility                 | Styles                       |
| ----------------------- | ---------------------------- |
| `self.auto`             | `align-self: auto;`          |
| `self['flex-start']`    | `align-self: flex-start;`    |
| `self['flex-end']`      | `align-self: flex-end;`      |
| `self['safe flex-end']` | `align-self: safe flex-end;` |
| `self.center`           | `align-self: center;`        |
| `self['safe center']`   | `align-self: safe center;`   |
| `self.stretch`          | `align-self: stretch;`       |
| `self.baseline`         | `align-self: baseline;`      |
| `self['last baseline']` | `align-self: last baseline;` |
