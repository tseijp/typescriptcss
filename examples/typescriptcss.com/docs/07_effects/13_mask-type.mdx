export const title = 'mask-type'
export const description = 'Utilities for controlling how an SVG mask is interpreted.'

## mask-type

Utilities for controlling how an SVG mask is interpreted.

| Utility          | Styles                  |
| ---------------- | ----------------------- |
| `mask.alpha`     | `mask-type: alpha;`     |
| `mask.luminance` | `mask-type: luminance;` |

{/* @MEMO mask.type.alpha, mask.type.luminance */}
